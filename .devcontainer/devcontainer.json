{
  "name": "claude-code-sandbox",
  "build": {
    "dockerfile": "Dockerfile"
  },

  // --- Host credential mounts (read-only) ---
  "mounts": [
    {
      "source": "${localEnv:HOME}/.ssh",
      "target": "/home/vscode/.ssh",
      "type": "bind",
      "readOnly": true
    },
    {
      "source": "${localEnv:HOME}/.config/gh",
      "target": "/home/vscode/.config/gh-host",
      "type": "bind",
      "readOnly": true
    },
    {
      "source": "${localEnv:HOME}/.claude",
      "target": "/home/vscode/.claude",
      "type": "bind"
    },
    {
      "source": "${localEnv:HOME}/.gitconfig",
      "target": "/home/vscode/.gitconfig-host",
      "type": "bind",
      "readOnly": true
    },
    {
      "source": "${localEnv:HOME}/.gnupg",
      "target": "/home/vscode/.gnupg-host",
      "type": "bind",
      "readOnly": true
    }
  ],

  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },

  // Extract gh token from host keychain before container starts
  "initializeCommand": "gh auth token > ~/.config/gh/.gh-token 2>/dev/null || true",

  "postCreateCommand": "bash .devcontainer/setup.sh",

  "remoteUser": "vscode",

  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/home/vscode/.claude",
    "GIT_CONFIG_GLOBAL": "/home/vscode/.gitconfig-local"
  },

  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}