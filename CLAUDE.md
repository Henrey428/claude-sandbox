# Claude Code Instructions

## Environment: Devcontainer

- GPG signing is not available in this container. Never use `--gpg-sign` or `-S` flags when committing. If a commit fails due to GPG signing, retry with `git -c commit.gpgsign=false commit`.
